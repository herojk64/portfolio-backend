root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/api"
  cmd = "go build -o ./tmp/api ./cmd/api"
  delay = 1000
  exclude_dir = ["tmp", "vendor", "sql", "config"]
  include_ext = ["go"]

[log]
  time = true

[screen]
  clear_on_rebuild = true
