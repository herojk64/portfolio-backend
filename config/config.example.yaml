app:
  name: backend
  env: development
  port: 8000

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: backend
  sslmode: disable

redis:
  host: localhost
  port: 6379

session:
  secret: supersecretkey
  expiry_minutes: 60

log:
  level: debug
